https://github.com/ZeroCho/nodejs-book

node.js는 크롬v8 자바스크립트 엔진으로 빌드된 자바스크립트 런타임입니다.

이벤트: 클릭, 네트워크 요청,타이머
이벤트 리스너 :이벤트를 등록하는 함수
콜백 함수 : 이벤트가 발생했을 때 실행될 함수

논블로킹: 오래걸리는 함수를 백그라운드로 보내서 다음 코드가 먼저 실행되게 학, 나중에 오래걸리는 함수를 실행
논 블로킹 방식 하에서 일부 코드는 백그라운드에서 병렬로 실행됨
나머지 코드는 블로킹 방식으로 실행됨

노드 프로세스는 멀티 스레드이지만 직접 다룰 수 있는 스레드는 하나이기 때문에 싱글 스레드라고 표현
노드는 주로 멀티 스레드 대신 멀티 프로세스 활용

싱글스레드라 주어진 일을 하나밖에 처리하지 못함

노드는 서버를 구성할 수 있게 하는 모듈 제공

1.호출스택 , 이벤트 루프

호출스택 , 백그라운드, 태스크 큐, 메모리
호출스택과 백그라운드는 동시에 실행된다.


const add2 = (x,y)=>{
    return x + y;
};

const add3 = (x,y) => x + y;
const add4 = (x,y) => (x + y);

function not1(x){
    return !x;
}

const not2 = x => !x;

const obj = (x,y)=> ({x,y})//객체를 리턴하는경우에는 소활호()가 필수
function는 자기만의 this를 갖는다.

화살표함수=> 를 쓰면 부모의 this 만 갖는다.

c?.d //c가없으면 넘어가고 c가있으면 c.d
a?.b?.c?.d?.e 이러면 에러는 안남..

?? : null 과 undefined 일때만 뒤로 넘어감  

자바스크립트 REPL Read Evaluate Print Loop
node: 자바스크립트 실행기








































